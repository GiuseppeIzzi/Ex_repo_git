-- Supponiamo di voler creare una tabella con gli ordini mensili per categoria di prodotto:
config {type: "table" }
SELECT
    p.category

FROM $ref{("products")} AS p
JOIN $ref{("orders")} AS o ON p.name = o.product
