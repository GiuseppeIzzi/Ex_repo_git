config {type: "table"}
SELECT
    c.client_id,
    c.name AS client_name,
    c.country,
    o.order_id,
    o.order_date,
    o.price AS order_amount
FROM $ref{("ecommerce_dataset.clients")} AS c
JOIN $ref{("ecommerce_dataset.orders")} AS o ON c.client_id = o.client_id
